#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        vector<char> ar(n);
        vector<int> main;
        int count = 0;
        for (int i = 0; i < n; i++)
        {
            cin >> ar[i];
        }
        
        for (int i = 0; i < n; i++)
        {
            if (ar[i] == '-')
            {
                 
            }
            
        }
    }
}